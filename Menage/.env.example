# Local development. On Railway, DATABASE_URL is provided automatically by the Postgres plugin.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/menage
PORT=3000

# Library starts EMPTY. Set to "true" only if you ever want to load the
# starter list (from seed-data.js) on first run.
SEED_DEFAULT=false
